apiVersion: v1
kind: Service
metadata:
  name: global-nodeport
  labels:
    app: dev
spec:
  type: NodePort  
  ports:
  - nodePort: 30087
    targetPort: 8087
    port: 8087
    protocol: TCP
  selector:
    app: simple
---
